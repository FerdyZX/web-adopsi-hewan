#!/bin/bash

# Build and run with Docker Compose
docker-compose up --build

# Or build separately
# docker build -t adopsi-hewan-frontend ./frontend
# docker build -t adopsi-hewan-backend ./backend
# docker-compose up
